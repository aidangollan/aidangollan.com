export interface Profile {
    id: number;
    email: string;
    githubLink: string;
    linkedinLink: string;
    resumeLink: string;
}

export interface Views {
    id: number;
    views: string;
}